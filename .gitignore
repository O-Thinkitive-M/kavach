# Kavach writes nothing into a repository any more — all project state lives
# under ~/.kavach/projects/. This entry only covers stores created by older
# versions, which are migrated on first run and then ignored.
.pr-architect/

node_modules/
*.log
.DS_Store
